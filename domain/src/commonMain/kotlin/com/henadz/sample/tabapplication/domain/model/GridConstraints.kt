package com.henadz.sample.tabapplication.domain.model

object GridConstraints {
    const val MIN_ROWS = 1
    const val MAX_ROWS = 1000
    const val MIN_COLS = 1
    const val MAX_COLS = 6
    val MAX_ROWS_INPUT_LENGTH: Int = MAX_ROWS.toString().length
    val MAX_COLS_INPUT_LENGTH: Int = MAX_COLS.toString().length
}
